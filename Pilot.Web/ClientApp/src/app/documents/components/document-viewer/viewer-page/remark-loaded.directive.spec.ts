import { RemarkLoadedDirective } from './remark-loaded.directive';

describe('RemarkLoadedDirective', () => {
  it('should create an instance', () => {
    const directive = new RemarkLoadedDirective();
    expect(directive).toBeTruthy();
  });
});
